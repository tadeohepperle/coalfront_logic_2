name: coalfront_logic_2
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0-179.0.dev <4.0.0'

# Add regular dependencies here.
dependencies: 
  dartz: ^0.10.1
  uuid: ^3.0.7
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0


analyzer:
  enable-experiment:
    - records
    - class-modifiers
    - sealed-class
    - patterns
    - inline-class
    - macros
    - const-functions
    - value-class
    - variance
    - extension-types
    - inference-update-2
